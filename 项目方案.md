# 制鞋机器人视觉系统 — 项目方案

> 福建省科技重大专项 | 2024.9 - 2026.3 | 1.5年
> 2项发明专利（第一/第二发明人）| 1篇论文（第一作者，在审）

---

## 一、项目背景

制鞋产线的涂胶工序长期依赖人工——工人手持胶枪沿鞋底边缘涂胶，效率低、质量不稳定、且胶水挥发对工人健康有害。自动化涂胶的核心难点是：每只鞋底的形状、尺码、材质都不同，固定轨迹的机器人无法适应"个性化鞋型"。

**项目目标**：输入鞋底的 RGB-D 图像 → 自动识别涂胶区域 → 重建三维轮廓 → 生成带位姿信息的涂胶轨迹 → 导引机器人执行。

---

## 二、为什么这不是简单的边缘检测

| 难点 | 具体表现 | 为什么难 |
|---|---|---|
| 伪装分割 | 鞋底与背景颜色/纹理接近，RGB图像中边界模糊 | 纯RGB分割容易漏检/误检，需要深度信息辅助 |
| 个性化鞋型 | 每款鞋的涂胶区域形状不同，无法预定义模板 | 模型必须泛化到未见过的鞋型 |
| 三维轮廓连续性 | 涂胶轨迹必须连续、闭合、平滑，不能有断点 | 点云噪声/缺失会导致轮廓断裂，直接影响涂胶质量 |
| 位姿信息 | 机器人不仅需要轨迹点的位置(x,y,z)，还需要法向量(姿态) | 涂胶枪需要垂直于鞋底曲面，姿态错误会导致涂胶不均 |
| 实时性 | 产线节拍要求快速推理 | 模型需轻量化，从图像到轨迹全流程需在产线可接受时间内完成 |
| 精度要求 | 涂胶轨迹定位精度 ≤1mm | 亚毫米级精度对视觉感知和点云处理都提出极高要求 |

**核心教训**：这个任务的难度不在于任何一个单点环节，而在于**全链路的误差累积**——分割差1像素，点云就偏一点，轨迹就偏一截，涂胶就不到位。每一步都必须做到足够精确，否则误差会级联放大。

---

## 三、三阶段架构设计

```
阶段一：实例分割（感知）
  RGB-D鞋底图像 → 轻量分割模型 → 鞋底涂胶区域像素级mask

阶段二：三维重建与点云处理（重建）
  分割mask + 深度信息 → 涂胶区域点云 → 多平面投影 + 几何特征融合 → 连续平滑的三维轮廓

阶段三：轨迹提取与工程落地（轨迹）
  三维轮廓 → 几何分析 + 工艺参数 → 带位姿的涂胶轨迹 → 模型轻量化 → 产线部署
```

三阶段递进，每一步以上一步的输出为输入。核心方法学：**感知→重建→轨迹是递进关系，每一步的上游质量决定下游上限**。

---

## 四、阶段一：RGB-D 伪装实例分割

### 4.1 为什么鞋底分割是"伪装"问题

鞋底放在工作台上采集图像时，鞋底颜色与工作台背景颜色可能接近，在 RGB 图像中边界模糊——这在计算机视觉中属于**伪装目标分割（Camouflaged Object Segmentation）**问题。

纯 RGB 分割在这种场景下容易失败：鞋底和背景的纹理、颜色相似，CNN 难以提取有效的判别特征。解决方案是**引入深度信息**——鞋底和工作台在 Z 轴上有高度差，深度图中边界清晰。

### 4.2 RGB-D 双模态分割方案

| 模态 | 作用 | 优势 |
|---|---|---|
| RGB图像 | 提取纹理/颜色特征 | 鞋底表面纹路、品牌标识 |
| 深度图像 | 提供Z轴高度差信息 | 鞋底与背景的深度边界清晰 |
| 融合 | RGB特征 + 深度特征联合编码 | 互补：RGB管"是什么"，深度管"在哪" |

### 4.3 轻量分割模型设计

**动态路由**：根据输入鞋型的尺度分布，动态选择特征传播路径。不同鞋型（运动鞋/皮鞋/靴子）的涂胶区域形状和尺度差异大，静态网络结构难以兼顾——动态路由让网络自适应不同鞋型。

**拓扑聚合**：利用鞋底轮廓的拓扑结构信息（闭合性、连通性）增强特征聚合。鞋底涂胶区域是一个连续闭合的环状结构，拓扑约束能帮助模型生成更完整的分割 mask。

### 4.4 分割结果

- 识别准确率 >99%（产线验收级别）
- 模型经轻量化设计，满足产线实时性要求
- 详见论文《基于动态路由与拓扑聚合的实例分割网络》（第一作者，在审）

---

## 五、阶段二：三维重建与点云处理

### 5.1 从分割 mask 到点云

分割得到涂胶区域的像素级 mask 后，结合深度相机的内参矩阵，将 mask 区域的像素反投影到三维空间，生成涂胶区域的点云。

### 5.2 单视图三维重建

传统方法需要多视角图像做三维重建，但产线上只有一台固定相机（单视图）。单视图重建的核心挑战是**深度歧义**——从单张图像无法直接恢复完整的三维结构。

**协同编码架构**：
- 可见光分支：提取鞋底的纹理/几何特征
- 激光/深度分支：提取鞋底的三维形貌特征
- 协同编码：两个分支在编码阶段交互融合，而非简单后期拼接

### 5.3 特征融合标定

| 步骤 | 做什么 | 技术细节 |
|---|---|---|
| 虚拟标定靶点 | 基于鞋底几何结构生成标定靶点 | 不需要物理标定板，用鞋底自身的几何特征 |
| 空间配准 | 可见光图像与三维点云对齐 | 利用包含几何结构信息的可见光图像与点云实现配准 |
| 深度学习标定 | 迭代更新标定参数 | 通过深度学习模型输出配准变换矩阵 |

### 5.4 多平面投影与几何特征融合

```
鞋底点云
    ↓
多平面投影：从不同方向对点云做切面，获取截面轮廓
    ↓
几何特征提取：每个截面的最高点/边缘点/曲率变化点
    ↓
特征融合：融合多个投影方向的几何特征
    ↓
轮廓拟合：拟合连续、平滑、闭合的三维涂胶轮廓
```

**为什么用多平面投影而非单方向扫描**：鞋底是复杂曲面，单方向扫描可能在某些曲率变化剧烈的区域丢失轮廓点。多平面投影从多个方向获取截面，融合后能覆盖鞋底的完整轮廓。

### 5.5 GAN 误差校正

基于涂胶轮廓与设计轨迹的几何偏差，利用生成对抗网络（GAN）在曲率连续性与涂胶工艺间隙的预设约束下生成修正轮廓。

| 校正维度 | 约束条件 | 目标 |
|---|---|---|
| 曲率连续性 | 轮廓曲率不能突变 | 保证涂胶轨迹平滑，机器人运动无抖动 |
| 涂胶工艺间隙 | 相邻轨迹点间距符合工艺要求 | 保证涂胶均匀，无漏涂/堆胶 |

---

## 六、阶段三：轨迹提取与工程落地

### 6.1 从轮廓到轨迹

三维涂胶轮廓是"线"，涂胶轨迹是"带位姿的点序列"——机器人需要每个轨迹点的 (x,y,z) 位置和法向量（姿态）。

| 步骤 | 做什么 | 输出 |
|---|---|---|
| 轮廓离散化 | 将连续轮廓按曲率自适应离散为点序列 | 曲率大处密、曲率小处疏 |
| 法向量计算 | 对每个点估计局部曲面法向量 | 涂胶枪姿态 |
| 工艺参数注入 | 结合涂胶宽度、间距、速度等工艺要求 | 最终轨迹点序列 |
| 坐标变换 | 从相机坐标系变换到机器人坐标系 | 机器人可执行的运动指令 |

### 6.2 模型轻量化与推理加速

| 优化 | 方法 | 效果 |
|---|---|---|
| 模型压缩 | 轻量backbone + 动态路由减少冗余计算 | 满足产线部署资源约束 |
| 推理加速 | 全链路优化（分割+点云+轨迹） | 满足产线实时性 |
| 全链路优化 | 分割+点云+轨迹全流程耗时优化 | **产线节拍 3s/只** |

### 6.3 产线部署验证

独立打通"数据采集→算法研发→产线部署验证"全流程：

```
数据采集：RGB-D相机采集鞋底图像（多鞋型/多尺码/多材质）
    ↓
算法研发：分割→点云→轨迹（离线开发+验证）
    ↓
产线部署：模型部署到产线工控机 + 机器人执行涂胶
    ↓
验证：涂胶轨迹定位精度 ≤1mm，识别准确率 >99%
```

**实验室到产线的精度衰减**：实验室里点云干净、光照可控、鞋底摆放标准；产线上光照变化导致 RGB 分割波动、鞋底材质反射导致点云噪声。应对方案：RGB-D 融合让模型对光照变化鲁棒；多平面投影的冗余采样抑制点云噪声；GAN 误差校正兜底。

---

## 七、专利与论文

### 7.1 专利1：基于视觉单线激光鞋底三维涂胶轮廓提取系统及方法（第一发明人，已授权）

申请号：CN202511339618.5 / 公开号：CN120852582A

**三大核心模块**：

| 模块 | 功能 | 核心创新 |
|---|---|---|
| 特征融合标定模块 | 视觉与激光数据空间配准 | 基于鞋底几何结构生成虚拟标定靶点，无需物理标定板；深度学习迭代更新配准变换矩阵 |
| 双模态轮廓提取模块 | 生成三维涂胶轮廓 | CAD模型先验 + 联合分割 + 几何纹理特征融合 → 连续闭合的三维轮廓 |
| 智能误差校正模块 | 轮廓修正 | GAN在曲率连续性与涂胶工艺间隙约束下生成修正轮廓 |

### 7.2 专利2：基于RGB-D伪装实例分割的鞋底涂胶区域点云生成方法（第二发明人，已授权）

申请号：CN202511331565.2 / 公开号：CN120823398B

**核心创新**：将"伪装目标分割"的概念引入工业场景——鞋底与背景颜色接近时，利用 RGB-D 的深度信息辅助分割，从分割结果直接生成涂胶区域点云。

### 7.3 论文：基于动态路由与拓扑聚合的实例分割网络（第一作者，在审）

**核心创新**：
- **动态路由**：根据输入图像的尺度分布动态选择特征传播路径，适应不同鞋型的涂胶区域尺度差异
- **拓扑聚合**：利用目标的拓扑结构信息（闭合性、连通性）增强特征聚合，生成更完整的分割mask

灵感来源：制鞋项目中遇到"不同鞋型涂胶区域尺度差异大"和"分割mask不闭合"两个问题，由此设计了动态路由+拓扑聚合的解决方案。

---

## 八、最终成果

| 指标 | 结果 |
|---|---|
| 识别准确率 | >99% |
| 涂胶轨迹定位精度 | ≤1mm |
| 产线节拍 | 3s/只 |
| 专利 | 2项授权（第一/第二发明人） |
| 论文 | 1篇在审（第一作者） |
| 部署验证 | 独立打通全流程，产线验证通过 |

---

## 九、和其他项目的方法学对比

| 维度 | CXMT（分类） | 讯飞（检测+定性） | 本项目（分割+3D+轨迹） | RAG（问答） |
|---|---|---|---|---|
| 任务类型 | 图像分类 | 目标检测+规则定性 | 实例分割+三维重建+轨迹生成 | RAG问答 |
| 输入 | 缺陷图 224×224 | X射线底片 >2000×2000 | RGB-D鞋底图像 | 学术文档 |
| 核心瓶颈 | 长尾分布 | 小目标+大图 | 伪装分割+点云连续性+全链路精度 | 检索质量+小模型 |
| 方法学 | 温和引导 | 数据策略>模型>loss | **感知→重建→轨迹递进** | 检索>生成>Prompt |
| 共同点 | 上游瓶颈决定系统上限 | 同左 | **同左——分割不准则点云不准则轨迹不准** | 同左 |

---

## 十、可复现性

> 本项目为福建省科技重大专项，代码和数据涉及项目保密协议，不公开。核心方法可在公开数据集上复现。

复现关键点：
- RGB-D伪装分割：参考 RGB-D Camouflaged Object Detection 相关工作
- 多平面投影：Open3D + 多方向截面采样 + 截面特征提取
- 动态路由：参考 Dynamic Routing for Semantic Segmentation（CVPR 2020），扩展到实例分割
- 轨迹提取：法向量估计 + 曲率自适应离散 + 坐标变换

---

## 十一、面试预演 Q&A

**Q1：为什么用RGB-D而不是纯RGB或纯3D？**
A：纯RGB在鞋底与背景颜色接近时会失败——这是伪装分割问题。纯3D能解决边界问题，但丢失了鞋底表面的纹理信息。RGB-D融合让深度管"边界在哪"，RGB管"哪块是涂胶区域"，互补解决。

**Q2：单视图三维重建怎么做？**
A：传统三维重建需要多视角，但产线上只有一台固定相机。专利1的解法是协同编码——可见光和激光/深度两个分支在编码阶段就交互融合，让网络从单视图中"推断"出完整的三维结构。配合多平面投影和GAN误差校正，精度足以满足≤1mm。

**Q3：≤1mm的精度怎么保证？**
A：三个环节共同保证：①分割精度——RGB-D融合+动态路由保证mask准确；②点云精度——多平面投影的冗余采样+GAN误差校正抑制噪声；③轨迹精度——曲率自适应离散+法向量精确估计。每个环节的误差都被控制在亚毫米级，级联后总精度≤1mm。

**Q4：产线部署遇到的最大问题是什么？**
A：实验室到产线的精度衰减。实验室里点云干净、光照可控，产线上光照变化、鞋底材质反射导致点云噪声。我的应对是：RGB-D融合让模型对光照变化鲁棒；多平面投影的冗余采样抑制点云噪声；GAN误差校正兜底。

**Q5：动态路由和拓扑聚合分别解决什么问题？**
A：动态路由解决"不同鞋型尺度差异大"——运动鞋和皮鞋的涂胶区域形状差异大，动态路由根据输入自适应选择路径。拓扑聚合解决"分割mask不闭合"——鞋底涂胶区域是闭合环，CNN可能输出有断点的mask，拓扑约束利用闭合性先验帮助生成完整轮廓。

**Q6：这个项目和你工业缺陷项目有什么关联？**
A：关联在"全链路精度"——CXMT是单点分类，讯飞是检测+定性的两阶段，本项目是分割+3D+轨迹的三阶段。三个项目的复杂度递增，但核心方法学一致：每一步的上游质量决定下游上限。本项目最深刻的是"误差级联放大"——分割差1像素，点云偏一点，轨迹偏一截，涂胶就不到位。

---

*本文为项目方案文档。详细技术笔记见：[cv-algorithm-notes/03-shoe-robot-vision.md](https://github.com/Aeijou37/cv-algorithm-notes/blob/main/articles/03-shoe-robot-vision.md)*
